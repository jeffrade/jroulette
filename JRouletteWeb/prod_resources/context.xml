<!-- The contents of this file will be loaded for each web application -->
<Context>

    <!-- Default set of monitored resources -->
    <WatchedResource>WEB-INF/web.xml</WatchedResource>
	
    <!-- Uncomment this to disable session persistence across Tomcat restarts -->
    <!--
    <Manager pathname="" />
    -->

    <!-- Uncomment this to enable Comet connection tacking (provides events
         on session expiration as well as webapp lifecycle) -->
    <!--
    <Valve className="org.apache.catalina.valves.CometConnectionManagerValve" />
    -->	
	<Resource name="jdbc/jroulette" auth="Container" type="javax.sql.DataSource"
               maxActive="100" maxIdle="30" maxWait="10000"
               username="jrade1_jrade1" password="qpalzm10" 
               driverClassName="com.mysql.jdbc.Driver"
               url="jdbc:mysql://localhost:3306/jrade1_jroulette"/>
               
	<!-- JavaMail session factory -->
	<Resource name="mail/Session"
              auth="Container"
              type="javax.mail.Session"
              username="support@jroulette.net"
              password="qpalzm10"
              mail.debug="true"
              mail.user="support@jroulette.net"
              mail.password="qpalzm10"
              mail.transport.protocol="smtp"
              mail.smtp.host="mail.jroulette.net"
              mail.smtp.auth="true"
              mail.smtp.port="25"
              mail.smtp.starttls.enable="false"/>

</Context>